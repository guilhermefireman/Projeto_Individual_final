<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= evento.nome %> - SambaPass</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background-color: #fff;
      color: #111;
      padding: 0;
    }

    .header {
      text-align: center;
      padding: 40px 20px 20px;
    }

    .header h1 {
      font-size: 2.8rem;
      margin-bottom: 10px;
      color: #2ecc71;
    }

    .banner {
      width: 100%;
      max-height: 480px;
      object-fit: cover;
      border-bottom: 8px solid #2ecc71;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 40px 20px;
      text-align: center;
    }

    .descricao {
      background-color: #f4f4f4;
      border-left: 6px solid #2ecc71;
      padding: 25px 20px;
      font-size: 1.1rem;
      color: #333;
      line-height: 1.6;
      margin-bottom: 40px;
      text-align: left;
      border-radius: 10px;
    }

    .info {
      display: flex;
      justify-content: space-around;
      flex-wrap: wrap;
      gap: 20px;
      font-size: 1rem;
      margin-bottom: 40px;
      color: #444;
    }

    .info-item {
      min-width: 150px;
    }

    .info-label {
      font-weight: bold;
      color: #2ecc71;
      display: block;
      margin-bottom: 5px;
    }

    .whatsapp-button {
      display: inline-block;
      background-color: #25D366;
      color: white;
      text-decoration: none;
      padding: 16px 36px;
      border-radius: 50px;
      font-weight: bold;
      font-size: 1.1rem;
      box-shadow: 0 0 10px rgba(37, 211, 102, 0.3);
      transition: background-color 0.3s ease, transform 0.2s ease;
    }

    .whatsapp-button:hover {
      background-color: #1ebe5d;
      transform: scale(1.05);
    }

    .back-link {
      display: block;
      margin-top: 50px;
      color: #555;
      font-size: 0.9rem;
      text-decoration: none;
    }

    .back-link:hover {
      text-decoration: underline;
    }

    @media (max-width: 600px) {
      .header h1 {
        font-size: 2rem;
      }

      .descricao {
        font-size: 1rem;
        padding: 20px 15px;
      }

      .whatsapp-button {
        font-size: 1rem;
        padding: 14px 28px;
      }

      .info {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= evento.nome %></h1>
  </div>

  <% if (evento.imagem_url) { %>
    <img src="<%= evento.imagem_url %>" alt="Imagem do evento" class="banner">
  <% } %>

  <div class="container">
    <div class="descricao">
      <%= evento.descricao %>
    </div>

    <div class="info">
      <div class="info-item">
        <span class="info-label">Data</span>
        <%= new Date(evento.data).toLocaleString('pt-BR') %>
      </div>
      <div class="info-item">
        <span class="info-label">Local</span>
        <%= evento.local %>
      </div>
      <div class="info-item">
        <span class="info-label">Cidade</span>
        <%= evento.cidade %>
      </div>
    </div>

    <% if (evento.whatsapp_link) { %>
      <a href="<%= evento.whatsapp_link %>" class="whatsapp-button" target="_blank">
        Comprar Ingresso no WhatsApp
      </a>
    <% } %>

    <a href="/eventos/<%= evento.cidade %>" class="back-link">← Voltar para eventos de <%= evento.cidade %></a>
  </div>
</body>
</html>
