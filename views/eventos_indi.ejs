<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= evento.nome %> - SambaPass</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/eventos_indi.css" />
</head>
<body>

  <header>
    <h1><%= evento.nome %></h1>
  </header>

  <div class="banner-container">
    <% if (evento.imagem_url) { %>
      <img src="<%= evento.imagem_url %>" alt="Imagem do evento">
    <% } %>
  </div>

  <div class="container">
    <div class="descricao">
      <%= evento.descricao %>
    </div>

    <div class="info">
      <div class="info-item">
        <span>Data</span>
        <%= new Date(evento.data).toLocaleString('pt-BR') %>
      </div>
      <div class="info-item">
        <span>Local</span>
        <%= evento.local %>
      </div>
      <div class="info-item">
        <span>Cidade</span>
        <%= evento.cidade %>
      </div>
    </div>

    <% if (evento.whatsapp_link) { %>
      <div style="text-align: center;">
        <a href="<%= evento.whatsapp_link %>" class="whatsapp-button" target="_blank">
          Comprar Ingresso no WhatsApp
        </a>
      </div>
    <% } %>

    <a href="/eventos/<%= evento.cidade %>" class="back-link">
      ← Voltar para eventos de <%= evento.cidade %>
    </a>
  </div>

</body>
</html>
