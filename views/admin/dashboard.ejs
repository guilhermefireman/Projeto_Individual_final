<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Painel Admin - SambaPass</title>
  <link rel="stylesheet" href="/css/dashboard.css" />
</head>
<body>
  <header>
    <div class="titulo">Painel Admin — SambaPass</div>
    <form method="GET" action="/admin/logout">
      <button type="submit">Logout</button>
    </form>
  </header>

  <main>
    <h1>Bem-vindo, <%= nome %></h1>

    <div class="info">
      <p>Total de eventos: <strong><%= total %></strong></p>
      <p>Cidades cadastradas:</p>
      <ul>
        <% cidades.forEach(cidade => { %>
          <li><%= cidade %></li>
        <% }) %>
      </ul>
    </div>

    <a href="/admin/eventos">Gerenciar Eventos</a>
  </main>
</body>
</html>
