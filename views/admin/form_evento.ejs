<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= evento ? 'Editar' : 'Novo' %> Evento</title>
  <link rel="stylesheet" href="/css/form_evento.css" />
</head>
<body>
  <h1><%= evento ? 'Editar' : 'Novo' %> Evento</h1>

  <form method="POST" action="<%= evento ? '/admin/eventos/' + evento.id : '/admin/eventos' %>">
    <label for="nome">Nome</label>
    <input type="text" name="nome" value="<%= evento ? evento.nome : '' %>" required />

    <label for="descricao">Descrição</label>
    <textarea name="descricao" rows="4"><%= evento ? evento.descricao : '' %></textarea>

    <label for="data">Data</label>
    <input type="datetime-local" name="data" value="<%= evento ? new Date(evento.data).toISOString().slice(0,16) : '' %>" required />

    <label for="local">Local</label>
    <input type="text" name="local" value="<%= evento ? evento.local : '' %>" required />

    <label for="cidade">Cidade</label>
    <input type="text" name="cidade" value="<%= evento ? evento.cidade : '' %>" required />

    <label for="imagem_url">URL da Imagem</label>
    <input type="text" name="imagem_url" value="<%= evento ? evento.imagem_url : '' %>" />

    <label for="whatsapp_link">Link do WhatsApp</label>
    <input type="text" name="whatsapp_link" value="<%= evento ? evento.whatsapp_link : '' %>" />

    <button type="submit">Salvar</button>
  </form>
</body>
</html>
