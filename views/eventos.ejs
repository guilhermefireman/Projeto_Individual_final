<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Eventos<%= cidade ? ' em ' + cidade.charAt(0).toUpperCase() + cidade.slice(1) : '' %></title>
  <link rel="stylesheet" href="/css/eventos.css" />
</head>
<body>
  <header>
    <h1>Eventos<%= cidade ? ' em ' + cidade.charAt(0).toUpperCase() + cidade.slice(1) : '' %></h1>
  </header>

  <main class="container">
    <!-- Substituí o EJS por uma div onde o JS vai inserir os eventos -->
    <div id="eventos" class="cards"></div>
  </main>

  <!-- Script que busca e exibe os eventos automaticamente -->
  <script src="/js/main.js"></script>
</body>
</html>
